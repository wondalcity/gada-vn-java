# Expo
.expo/
dist/
web-build/

# Native build output (generated by expo prebuild — do not commit)
android/
ios/

# Keystores (never commit — store securely)
*.keystore
*.jks
google-play-service-account.json

# Firebase config (sensitive — add manually per environment)
google-services.json
GoogleService-Info.plist

# Node
node_modules/

# Environment
.env.local
.env.*.local

# OS
.DS_Store
